---
interface Props {
  className?: string;
  id?: string;
}

const { className = '', id } = Astro.props;
---

<section id={id} class={`section-container ${className}`}>
  <div class="container mx-auto px-4">
    <slot />
  </div>
</section>

<style>
  /* スクロール問題を解決するためにアニメーションを完全に無効化 */
  .section-container {
    /* アニメーションなしで、スクロール問題を回避 */
  }
</style>

<!-- スクロール問題を回避するためにスクリプトを無効化 -->
